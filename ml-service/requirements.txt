# ─── Production dependencies ────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.6
python-multipart>=0.0.9
# Use headless build (no GUI) — works in Docker without a display
opencv-contrib-python-headless>=4.10.0
numpy>=1.26.4
pydantic>=2.9.2
pillow>=10.4.0

# Optional / not currently imported by the pipeline. rasterio pulls a heavy
# GDAL toolchain into the image and is the most common cause of a failed
# Docker build, so it is disabled by default. Re-enable (and restore the
# gdal apt packages in the Dockerfile) only if you add GeoTIFF I/O.
# rasterio>=1.3.10
# scipy>=1.13.1
# scikit-image>=0.24.0

# ─── Dev / test dependencies ─────────────────────────────────────────────────
pytest>=8.3.3
httpx>=0.27.2
