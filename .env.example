# Copy to `.env` (already committed with these defaults) and adjust if the
# host ports 3000 / 8000 / 8001 are already taken on your machine.

BACKEND_PORT=8000
ML_SERVICE_PORT=8001
FRONTEND_PORT=3000
